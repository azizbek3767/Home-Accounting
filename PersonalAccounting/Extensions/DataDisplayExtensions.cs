using Microsoft.EntityFrameworkCore;
using Microsoft.Extensions.Options;
using PersonalAccounting.DataAccess;
using PersonalAccounting.Models;
using PersonalAccounting.Models.Repositories;

namespace PersonalAccounting.Extensions
{
    public static class DataDisplayExtensions
    {
        /*public static string GetCategoryNameByTransactionId2(int id)
        {
            Appdb
            Helper helper = new Helper(new AppDbContext(options => options.UseSqlServer(configuration.GetConnectionString("PersonalAccountingDb"))));
        }*/
    }
}
